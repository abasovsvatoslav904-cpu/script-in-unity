using UnityEngine;

public class HelloyWorld : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("Helloy World"); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
