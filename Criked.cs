using UnityEngine;

public class Criked : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print("criked");
    }

    // Update is called once per frame
    void Update()
    {
        print("criked");
    }
}
